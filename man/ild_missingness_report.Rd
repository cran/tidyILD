% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ild_missingness_report.R
\name{ild_missingness_report}
\alias{ild_missingness_report}
\title{Missingness workflow report (orchestration)}
\usage{
ild_missingness_report(
  x,
  outcome,
  predictors = NULL,
  fit_missing_model = TRUE,
  random = FALSE,
  expected_occasions = NULL,
  max_ids = NULL,
  seed = NULL,
  cohort_plot = TRUE
)
}
\arguments{
\item{x}{An ILD object (see [is_ild()]).}

\item{outcome}{Character; outcome column with possible `NA`.}

\item{predictors}{Optional character vector passed to [ild_missing_model()]
when `fit_missing_model` is `TRUE`. If empty, no missingness model is fit.}

\item{fit_missing_model}{Logical; fit [ild_missing_model()] when predictors
are non-empty.}

\item{random}{Logical; passed to [ild_missing_model()] (`glmer` vs `glm`).}

\item{expected_occasions}{Optional integer for [ild_missing_compliance()].}

\item{max_ids, seed}{Passed to [ild_missing_pattern()] for large cohorts.}

\item{cohort_plot}{Logical; include ggplot for [ild_missing_cohort()].}
}
\value{
A list (class `ild_missingness_report`) with:
  \describe{
    \item{compliance}{Tibble from [ild_missing_compliance()].}
    \item{pattern}{Output of [ild_missing_pattern()] (includes compliance
      columns on `by_id` if `outcome` was passed through).}
    \item{cohort}{List from [ild_missing_cohort()].}
    \item{hazard}{Tibble from [ild_missing_hazard_first()].}
    \item{flags}{Named list: `dropout_late_pooled` (logical from same
      heuristic as guardrail `GR_DROPOUT_LATE_CONCENTRATION`).}
    \item{missing_model}{Result of [ild_missing_model()] or `NULL`.}
    \item{snippets}{Character vector of short paragraphs for copy-paste.}
  }
}
\description{
Bundles person-level compliance, pattern summaries, cohort and hazard tables,
optional [ild_missing_model()], heuristic flags, and short text snippets for
methods sections. This is **diagnostic and reporting** tooling, not a substitute
for formal sensitivity analysis or MNAR models.
}
\seealso{
[ild_missing_pattern()], [ild_missing_bias()], [ild_ipw_weights()],
  `vignette("ild-missingness-workflow", package = "tidyILD")`

Other ild_diagnostics_utilities: 
\code{\link{ild_design_check}()},
\code{\link{ild_ipcw_weights}()},
\code{\link{ild_iptw_msm_weights}()},
\code{\link{ild_iptw_weights}()},
\code{\link{ild_ipw_weights}()},
\code{\link{ild_joint_msm_weights}()},
\code{\link{ild_missing_cohort}()},
\code{\link{ild_missing_compliance}()},
\code{\link{ild_missing_hazard_first}()},
\code{\link{ild_missing_model}()},
\code{\link{ild_missing_pattern}()}
}
\concept{ild_diagnostics_utilities}
