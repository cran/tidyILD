% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ild_missing_extensions.R
\name{ild_missing_compliance}
\alias{ild_missing_compliance}
\title{Person-level missingness compliance for one outcome}
\usage{
ild_missing_compliance(x, outcome, expected_occasions = NULL)
}
\arguments{
\item{x}{An ILD object (see [is_ild()]).}

\item{outcome}{Character; column with possible `NA` (e.g. EMA outcome).}

\item{expected_occasions}{Optional positive integer: planned waves per person
for adherence (`pct_of_expected`). If persons have different row counts,
this is only a rough benchmark; see Details.}
}
\value{
A tibble with columns `.ild_id`, `n_rows`, `n_obs_outcome`,
  `pct_nonmissing_outcome`, `longest_run_observed`, `monotone_missing`
  (`NA` if no missing values on the outcome for that person), and if
  `expected_occasions` is set, `pct_of_expected` and `meets_expected_rows`.
}
\description{
Per person (after sorting by `.ild_seq`), returns coverage on the outcome,
longest streak of observed values, whether missingness is monotone (dropout
pattern: once missing, stays missing), and optional comparison to an expected
number of occasions.
}
\details{
**Monotone missing** is defined only when the person has at least one missing
outcome: all observations after the first missing are missing. Intermittent
missing yields `FALSE`. **Discrete hazard** for first missing is provided by
[ild_missing_hazard_first()] (cohort-level by `.ild_seq`).
}
\seealso{
[ild_missing_pattern()], [ild_missingness_report()]

Other ild_diagnostics_utilities: 
\code{\link{ild_design_check}()},
\code{\link{ild_ipcw_weights}()},
\code{\link{ild_iptw_msm_weights}()},
\code{\link{ild_iptw_weights}()},
\code{\link{ild_ipw_weights}()},
\code{\link{ild_joint_msm_weights}()},
\code{\link{ild_missing_cohort}()},
\code{\link{ild_missing_hazard_first}()},
\code{\link{ild_missing_model}()},
\code{\link{ild_missing_pattern}()},
\code{\link{ild_missingness_report}()}
}
\concept{ild_diagnostics_utilities}
